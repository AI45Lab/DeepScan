"""
API package for optional webhook server and payload utilities.

This file exists to ensure `import api.main` works reliably under pytest's
import modes (including `--import-mode=importlib`).
"""

